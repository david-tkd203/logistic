# =============================================================================
# RestoLogistics — Variables de entorno
# =============================================================================
# USO:
#   Local:  cp .env.example .env  y ajustá valores
#   Dokploy: configurá CADA una de estas variables en la UI del proyecto
# =============================================================================


# ── Django ────────────────────────────────────────────────────────
# Generá una key única: python -c "import secrets; print(secrets.token_urlsafe(50))"
DJANGO_SECRET_KEY=cambiame_en_produccion
DJANGO_DEBUG=False

# Dominio/s donde se accede (separados por coma)
DJANGO_ALLOWED_HOSTS=.tudominio.com,localhost

# Orígenes confiables para CSRF (misma URL base que el frontend)
DJANGO_CSRF_TRUSTED_ORIGINS=https://tudominio.com


# ── Base de datos ────────────────────────────────────────────────
# DB_HOST:
#   - Local: db (cuando usás el override con PostgreSQL en Docker)
#   - Dokploy con BD externa: logisticapp-restologistics-iinhav (el host interno)
DB_HOST=db
DB_ENGINE=django.db.backends.postgresql
DB_NAME=restologistics
DB_USER=restologistics
DB_PASSWORD=cambiame_en_produccion
DB_PORT=5432


# ── PostgreSQL (SOLO para desarrollo local con override) ────────
POSTGRES_DB=restologistics
POSTGRES_USER=restologistics
POSTGRES_PASSWORD=cambiame_en_produccion


# ── CORS ─────────────────────────────────────────────────────────
CORS_ALLOWED_ORIGINS=https://tudominio.com
